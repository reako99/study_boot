management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-components: always
#      show-details: always
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
    git:
      mode: "full"
info:
  app:
    name: hello-actuator
    company: yh
logging:
  level:
    hello.controller: debug